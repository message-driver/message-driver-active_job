require 'spec_helper'

describe MessageDriver::ActiveJob do
  it 'has a version number' do
    expect(MessageDriver::ActiveJob::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
